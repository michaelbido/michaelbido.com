{
  "name": "simple-server",
  "version": "1.0.0",
  "description": "serves a single folder for static webpages",
  "main": "mainSiteApp.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Michael Bido",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.4",
    "serve-favicon": "^2.5.0"
  }
}
